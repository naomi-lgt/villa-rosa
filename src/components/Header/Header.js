import './Header.scss';

const Header = () => {
    return (
        <div className="header-container">
            <section>
                <span>home</span>
                <span>services</span>
                <span>contact</span>
            </section>
        </div>
    )
};

export default Header;
